## Project Overview

This is a Python project which goal is to extract wikipedia articles given a URL, and then parse their content into the Markdown format.

Images should be parsed to get their url.

Math notation should be transformed into Latex notation

The main logic should be saved under the `src` folder

Make use of libraries like beautiful soup 4, wikipedia, and others you find relevant

Notify me of the process